import { MidState } from '../types';
/**
 * State machine valid states
 */
export declare const ST: {
    START: string;
    EMAIL_ENTERED: string;
    LOGIN_INFO_FETCH: string;
    LOGIN_INFO_FAIL: string;
    TENANT_FETCH: string;
    TENANT_SELECT: string;
    CONFIG_COMPLETE: string;
    LOGIN_REDIRECT: string;
    LOGIN: string;
    NO_TENANT_FOUND: string;
    FAIL: string;
    PASSWORD_ENTERED: string;
    FORGOT_PASSWORD: string;
};
export declare const ERROR_MSG = "ERROR_MSG";
export declare const PHASE = "PHASE";
export declare const DATA = "DATA";
export declare const MID = "MID";
/**
 * Move the state machine to the next state by merging the updated properties in `next` to
 * `current`
 *
 * @param current - the current mid instance
 * @param next - the changes to apply to `current` to obtain the next state
 * @return a promise that resolves when the state machine transitions
 */
export declare const nextState: (current: MidState, next: MidState) => Promise<MidState>;
